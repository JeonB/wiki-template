# Wiki Template 설계 개선사항

## 구현 완료된 개선사항

### 1. 검색 기능 추가 ✅
- **검색 모달**: Dialog 기반 검색 인터페이스
- **실시간 검색**: 입력 시 300ms 디바운싱으로 검색 실행
- **검색 범위**: 제목, 설명, 태그, 카테고리에서 검색
- **검색 결과 하이라이트**: 검색어 하이라이트 표시
- **키보드 단축키**: Cmd/Ctrl + K로 검색 모달 열기/닫기
- **키보드 네비게이션**: ↑↓ 방향키로 결과 탐색, Enter로 선택

### 2. UI/UX 개선 ✅
- **헤더 개선**: 목록 버튼 제거, 검색 버튼 추가
- **접근성**: 모든 버튼에 aria-label 추가
- **포커스 관리**: 검색 모달 열릴 때 자동 포커스

### 3. Suspense + 로딩 스켈레톤 ✅
- **`app/(wiki)/loading.tsx`**: 목록/레이아웃 로딩 시 네비·사이드바·카드 그리드 스켈레톤
- **`app/(wiki)/[slug]/loading.tsx`**: 문서 상세 로딩 시 제목·메타·본문 스켈레톤
- **`components/ui/skeleton.tsx`**: shadcn 스타일 Skeleton 컴포넌트

## 개선 제안 3가지 (우선 검토)

| 순위 | 제안 | 이유 | 채택 가능성 |
|------|------|------|------------|
| 1 | **Suspense + 로딩 스켈레톤** | 즉시 체감되는 UX 개선, 의존성 없음, App Router 권장 패턴, Phase 1 명시 | **높음** → 구현 완료 |
| 2 | **에러 바운더리 (`error.tsx`)** | 프로덕션 안정성, `not-found`만 있고 `error.tsx` 없음, App Router 표준 | **높음** |
| 3 | **코드 블록 구문 강조** | 기술 위키에서 코드 가독성 향상, `remark-html`만 사용 중이라 rehype-highlight 등 도입 필요 | **중간** |

## 추가 개선 제안사항

### 1. 성능 최적화

#### 1.1 검색 성능
- **인덱싱**: 문서가 많아질 경우 검색 인덱스 구축 고려
- **캐싱**: 검색 결과 캐싱 (같은 쿼리 재검색 시)
- **가상화**: 검색 결과가 많을 때 가상 스크롤링

#### 1.2 페이지네이션
- 문서 목록이 많을 때 페이지네이션 추가
- 무한 스크롤 옵션

#### 1.3 이미지 최적화
- 마크다운 내 이미지 업로드 기능 추가 시 Next.js Image 컴포넌트 사용

### 2. 사용자 경험 개선

#### 2.1 검색 기능 확장
- **본문 내용 검색**: 마크다운 본문 내용까지 검색 범위 확장
- **고급 검색**: 필터 옵션 (카테고리, 태그별 필터링)
- **검색 히스토리**: 최근 검색어 저장 (localStorage)

#### 2.2 네비게이션 개선
- **최근 문서**: 최근 본 문서 목록 (localStorage 기반)
- **카테고리 사이드바**: 카테고리별 문서 목록 표시
- **태그 클라우드**: 인기 태그 표시 및 클릭 시 필터링
- **빵부스러기 네비게이션**: 현재 위치 표시

#### 2.3 문서 관리
- **문서 히스토리**: Git 기반 변경 이력 표시 (선택적)
- **문서 복사**: 문서 복제 기능
- **문서 이동**: 카테고리 변경 기능

### 3. 코드 구조 개선

#### 3.1 에러 처리
- **에러 바운더리**: 통합된 에러 처리 컴포넌트
- **에러 메시지**: 사용자 친화적인 에러 메시지
- **로딩 상태**: Suspense 및 로딩 스켈레톤 추가

#### 3.2 타입 안전성
- **엄격한 타입**: 더 구체적인 타입 정의
- **타입 가드**: 런타임 타입 검증

#### 3.3 유틸리티 함수
- **날짜 포맷팅**: 날짜 표시 유틸리티
- **슬러그 검증**: 더 엄격한 슬러그 검증

### 4. 접근성 개선

#### 4.1 키보드 네비게이션
- **전역 단축키**: 추가 키보드 단축키 (예: 'N'으로 새 문서)
- **포커스 트랩**: 모달 내 포커스 관리

#### 4.2 스크린 리더
- **ARIA 레이블**: 모든 인터랙티브 요소에 적절한 레이블
- **랜드마크**: 시맨틱 HTML 구조 개선

### 5. 기능 확장

#### 5.1 문서 기능
- **이미지 업로드**: 마크다운 내 이미지 업로드
- **파일 첨부**: PDF, 문서 파일 첨부
- **코드 하이라이팅**: 코드 블록 구문 강조

#### 5.2 협업 기능
- **댓글 시스템**: 문서별 댓글 기능 (선택적)
- **문서 공유**: 특정 문서 공유 링크 생성
- **변경 알림**: 문서 변경 시 알림 (선택적)

#### 5.3 관리 기능
- **백업/복원**: 문서 백업 및 복원 기능
- **내보내기**: 전체 문서를 마크다운 파일로 내보내기
- **통계**: 문서 통계 대시보드

### 6. 보안 및 안정성

#### 6.1 입력 검증
- **XSS 방지**: 마크다운 렌더링 시 XSS 방지 강화
- **파일명 검증**: 안전한 파일명 검증

#### 6.2 데이터 무결성
- **파일 잠금**: 동시 편집 방지 (선택적)
- **변경 감지**: 편집 중 다른 사용자의 변경 감지

## 우선순위별 개선 로드맵

### Phase 1 (즉시 적용 가능)
- ✅ 검색 기능 추가
- ✅ 헤더 UI 개선
- ✅ 로딩 스켈레톤 추가
- ✅ 에러 바운더리 추가 (`app/error.tsx`, `app/(wiki)/error.tsx`, `components/error-fallback.tsx`)

### Phase 2 (단기) ✅
- ✅ 본문 내용 검색 추가 (searchWikiPages에 matter로 본문 검색, 검색 모달 설명 문구 반영)
- ✅ 카테고리 필터링 (`/?category=`, CategoryFilter, 목록 필터)
- ✅ 검색 히스토리 (useSearchHistory, 검색 모달 최근 검색어·기록 지우기)

### Phase 3 (중기)
- 이미지 업로드
- 문서 히스토리 (Git 기반)
- 태그 클라우드
- 페이지네이션

### Phase 4 (장기)
- 댓글 시스템
- 문서 공유
- 통계 대시보드
- 백업/복원 기능

